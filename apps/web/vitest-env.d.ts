// Makes the @testing-library/jest-dom matchers (toBeInTheDocument, …) visible
// to TypeScript in component tests. Runtime registration happens in the shared
// vitest setup file.
import "@testing-library/jest-dom/vitest";
