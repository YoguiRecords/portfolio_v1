# Handoff nocturne — 2026-06-29 (run autonome)

> Points nécessitant ta décision/attention au réveil. Le reste de l'état courant
> est dans `.claude/PROGRESS.md` et les patch notes `docs/patch_notes/`.

## ✅ Livré cette nuit (sur `llm`, CI verte)
P0 (outillage test) · P1 (DB agenda/média/programmation/RDV) · P2 (home publique) ·
P3 (fiches projet, 12 blocs) · P4 (news + agenda + cron) · P5 (témoignages).
Bonus : convertisseur **image-processor** d'OXO remonté sur Docker, **avatar** (ta photo)
convertie en webp → MinIO → DB → affichée (duotone or).
**62 tests Vitest + E2E Playwright verts.** Vérifié visuellement (ultra-wide / FHD / mobile).

## 🔴 À TRAITER (sécurité)
1. **`CRON_SECRET=TrololoLeDev`** : tu l'as collé dans le chat + il est dans `.env`. Secret
   faible et exposé → **à régénérer** (valeur forte aléatoire). Il protège `/api/cron/publish`.

## ⚠️ Décisions à valider
2. **P4 avant Pi18n (incohérence de plan)** : P4 référence des routes `[locale]` mais Pi18n est
   planifié *après*. → J'ai construit News/Agenda en **FR** sur `/actus` et `/agenda`. **Pi18n
   devra restructurer** ces routes (et `/projets`, `/temoignages`) en `app/[locale]/…`.
3. **CSS Modules (CODE-STANDARDS)** : à partir de P3 je respecte « 1 composant + 1 CSS Module ».
   **P2 (home) utilise encore le CSS global** (`globals.css`) — dette à refactorer si tu veux
   l'alignement strict. Fonctionnel et fidèle à la DA, mais non conforme au standard modulaire.
4. **Convertisseur Node `services/converter`** : remplacé par `image-processor` (Python/OXO) dans
   `docker-compose.yml`. Le dossier `services/converter/` est encore dans le repo (mort) → à
   supprimer proprement (c'est un package workspace, voir TASKS).
5. **`AdminImageUploader` (Vue/OXO)** : à reporter en **React** pour le BO (P11). Plan détaillé :
   `docs/plans/2026-06-29-Pconv-image-processor.md`.

## ℹ️ Signalé par toi / à diagnostiquer
6. **`minio-init` ne se lance pas** : noté. **Non bloquant** — le bucket `media` est déjà public
   en lecture (vérifié : `GET /media/profile.webp` → 200). À diagnostiquer (probable one-shot déjà
   terminé / dépendance). Tâche dans `TASKS.md`.

## 🟡 Avatar (provisoire)
7. Ta photo a été convertie via **sharp** (manuel) puis poussée dans MinIO, et le seed crée le
   `MediaAsset` + lie `Profile.avatar` (URL via `MEDIA_PUBLIC_BASE_URL`, défaut `localhost:9100`).
   → À **re-générer via `image-processor`** une fois l'upload BO (P11) livré, pour le flux complet.
   Sur un MinIO vierge (CI/clone), l'objet n'existe pas → l'avatar tombe sur le monogramme (fallback).

## ⏭️ Reste à faire (ordre)
Pi18n (bilingue + `[locale]`) · P6 (contact) · P7 (SEO/AEO) · P8–P13 (back office) ·
P14–P15 (IA OpenRouter) · P16 (réseaux, *plan only*). Backlog détaillé : `.claude/TASKS.md`.

## ▶️ Vérifier en local
```bash
# DB de dev seedée + serveur web (déjà lancé cette nuit sur :3100)
export DATABASE_URL="postgresql://portfolio:portfolio_dev@localhost:5436/portfolio?schema=public"
pnpm --filter @portfolio/db exec tsx prisma/seed-content.ts   # contenu de démo
pnpm --filter web dev                                          # http://localhost:3100
pnpm test            # 62 tests (sérialisés)
pnpm test:e2e        # Playwright (home, projet, actus, agenda, témoignages)
```
Pages : `/` · `/projets/domestic-revolt` · `/actus` · `/actus/build-solo` · `/agenda` ·
`/agenda/meetup-dev-produit` · `/temoignages`.
