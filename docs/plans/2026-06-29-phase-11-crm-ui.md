# Phase 11 — CRM : UI — Plan d'implémentation

> **For Claude:** REQUIRED SUB-SKILL `superpowers:executing-plans`. Phase 11 de la roadmap. Pré-requis : **P10 mergée** (modèles + actions CRM), **P0/P1** (primitives + shell). Lien utile : **P9** (inbox) et **P7** (témoignages) qui posaient des hooks « créer contact ».

**Goal :** écrans CRM complets — **Contacts** (liste + fiche 360°), **Sociétés**, **Pipeline** (board par stage, glisser-déposer), **activités / notes / tâches-relances**. Activer les hooks « créer contact » depuis l'inbox et la modération.

**Architecture :** nouvelles pages `(dashboard)` + `components/crm/*`, branchées sur les actions de P10 ; fiche 360° agrège mails/messages (par email), projets, témoignages liés.

---

### Task 1 : Analyser le code précédemment développé
- Lire : `lib/crm/*` + `lib/actions/crm-actions.ts` (P10), `lib/inbox/aggregate.ts` (P9), `lib/content/moderation.ts` (P7), primitives `components/ui/*`.
- Extraire : signatures CRUD, comment relier un contact aux mails/messages (email) et témoignages.
- **Sortie :** plan de la fiche 360° (sources à agréger).

### Task 2 : Liste Contacts
**Files:** Create `app/(dashboard)/contacts/page.tsx` + `components/crm/contacts-table.tsx` (+ test)
- `DataTable` (nom, société, email, statut, dernière activité, actions) ; recherche ; filtres statut ; suppression confirmée. Commit `feat(admin): CRM contacts list`.

### Task 3 : Fiche contact 360°
**Files:** Create `app/(dashboard)/contacts/[id]/page.tsx` + `components/crm/*` (+ test)
- Infos contact + société ; **timeline d'activités** ; **deals** liés ; **mails/messages** liés (par email, via inbox) ; **projets/témoignages** associés ; ajout d'activité/note ; **tâches/relances** (échéance, fait).
- **TDD** : ajouter une note l'affiche dans la timeline (action mockée). Commit `feat(admin): contact 360 view`.

### Task 4 : Sociétés
**Files:** Create `app/(dashboard)/societes/page.tsx` (+ test)
- Liste + fiche société (contacts + deals rattachés). Commit `feat(admin): CRM companies`.

### Task 5 : Pipeline (board)
**Files:** Create `app/(dashboard)/pipeline/page.tsx` + `components/crm/pipeline-board.tsx` (+ test)
- Colonnes par `DealStage` ; cartes deal ; **glisser-déposer** change le stage (action update) ; totaux par colonne.
- **TDD** : déplacer une carte appelle l'update avec le nouveau stage (action mockée). Commit `feat(admin): CRM pipeline board`.

### Task 6 : Activer les hooks « créer contact »
**Files:** Modify inbox (P9) + témoignages (P7)
- Bouton « Créer un contact » → pré-remplit depuis l'expéditeur / l'auteur, crée via action CRM. Commit `feat(admin): create CRM contact from inbox/testimonial`.

### Task 7 : Barrière qualité + PR
- Tests verts ; typecheck/lint. **Sécu :** toutes les pages sous guard ; CRM jamais exposé public. Docs (`ARCHITECTURE`/`API_REFERENCE`). PR « feat(admin): CRM UI (P11) ».

## Definition of Done
- [ ] Contacts (liste + 360°), Sociétés, Pipeline (drag), activités/relances opérationnels.
- [ ] Hooks « créer contact » actifs (inbox + témoignages). Tests verts. Docs + PR.
