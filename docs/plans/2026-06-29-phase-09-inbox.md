# Phase 9 — Boîte de réception unique (Mails + Messages) — Plan d'implémentation

> **For Claude:** REQUIRED SUB-SKILL `superpowers:executing-plans`. Phase 9 de la roadmap. Pré-requis : **P1 mergée**.

**Goal :** une **vue unique** agrège **Mails (Microsoft Graph) + Messages (`ContactMessage`)** — même geste *répondre ou pas* — filtrable par source. **Hors périmètre : RDV** (P8).

**Architecture :** **nouveau** port d'agrégation `lib/inbox/aggregate.ts` qui normalise `GraphMessage` et `ContactMessage` vers un `InboxItem` commun ; réutilise `graph-mailbox.ts`, `mail-actions.ts`, `mail-reply-form.tsx`. Les pages `mails`/`messages` sont **absorbées** par `inbox`.

---

### Task 1 : Analyser le code précédemment développé
- Lire : `app/(dashboard)/mails/{page,[id]/page}.tsx`, `lib/integrations/graph-mailbox.ts`, `graph-client.ts`, `demo-mailbox.ts`, `lib/actions/mail-actions.ts`, `components/mail-reply-form.tsx`, `app/(dashboard)/messages/page.tsx`, modèle `ContactMessage`.
- Extraire : forme `GraphMessage` (id, from, subject, receivedDateTime, isRead, body), API d'envoi/réponse Graph, forme `ContactMessage`, `factory.ts` (demo vs Graph selon env).
- **Sortie :** champs communs à mapper dans `InboxItem` + comment répondre à chaque source.

### Task 2 : Type + port d'agrégation
**Files:** Create `lib/inbox/aggregate.ts` (+ `aggregate.test.ts`)
- `type InboxSource = "MAIL" | "CONTACT"` ; `type InboxItem = { id; source; from; subject; preview; date; isRead; contactEmail }`.
- `aggregateInbox()` : récupère mails (Graph/demo) + messages (Prisma), normalise, **fusionne**, **trie par date desc**, applique un filtre source.
- **TDD** : Step1 test FAIL — fusion de 2 sources mockées triée par date ; filtre `MAIL` ne renvoie que les mails. Step2 FAIL. Step3 impl. Step4 PASS. Commit `feat(admin): unified inbox aggregation (mails + messages)`.

### Task 3 : Page boîte de réception
**Files:** Create `app/(dashboard)/inbox/page.tsx` + `components/inbox/*` (+ test)
- Liste `InboxItem` ; filtres `Segmented` `[Tous][✉ Mail][📨 Contact]` ; non-lus en gras ; marquer lu. Commit `feat(admin): unified inbox page`.

### Task 4 : Détail + réponse
**Files:** Create `app/(dashboard)/inbox/[source]/[id]/page.tsx` (+ test)
- Lecture d'un item ; **répondre** (Graph pour MAIL ; pour CONTACT → email via Graph vers `ContactMessage.email`) en réutilisant `mail-reply-form`. Commit `feat(admin): inbox item view + reply`.

### Task 5 : Absorber mails/messages + hook CRM
**Files:** Modify routing (`mails`/`messages` → redirect/inbox), nav (P1) → entrée « Boîte de réception »
- Rediriger les anciennes routes ; bouton « Créer un contact » (stub jusqu'à P11). Commit `refactor(admin): fold mails+messages into unified inbox`.

### Task 6 : Barrière qualité + PR
- `aggregate.test.ts` + `demo-mailbox.test.ts` (existant) verts ; typecheck/lint. **Sécu :** `ContactMessage` jamais exposé à `app_web` (inchangé). Vérif réponse email (compte de test). Docs (`ARCHITECTURE`). PR « feat(admin): unified inbox (P9) ».

## Definition of Done
- [ ] Mails + Messages dans une vue unique filtrable ; réponse email OK ; RDV **absents** (vérifié). Tests verts. Docs + PR.
