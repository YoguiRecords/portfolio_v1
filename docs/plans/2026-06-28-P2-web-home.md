# P2 — Web: Home rendue depuis la DB — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Construire la page d'accueil publique de `apps/web` (nav, hero, Profil, Écosystème, Parcours, Cap, Projets, footer) **entièrement alimentée par la DB** (rôle lecture `app_web`), en réutilisant le markup/DA validé de `mockups/site-v3.html`.

**Architecture:** Server Components Next.js 16. Une couche d'accès `lib/data/*` lit la DB via `@portfolio/db` (rôle `app_web`, `select` explicites, filtres `PUBLISHED`/`APPROVED`). Les sections sont des composants serveur ; le seul îlot client est le typewriter du hero. Styles/tokens portés depuis `mockups/shared.css`. **Aucun contenu en dur.**

**Tech Stack:** Next.js 16 (App Router, RSC), Prisma 7, Vitest+RTL, Playwright.

**Référence markup/DA :** `mockups/site-v3.html` (structure, classes, animations) + `mockups/shared.css` (tokens). Le composant Parcours = `mockups/parcours-demo7.html` (4 voies, axe temps, mobile).

---

### Task 1: Couche d'accès « home »

**Files:** Create `apps/web/lib/data/home.ts`, Test `apps/web/lib/data/home.test.ts`

**Step 1: Write the failing test** (sur la DB de test isolée + factories de P0)
```ts
import { afterAll, beforeEach, expect, test } from "vitest";
import { makeTestClient } from "@portfolio/db/testing/db";
import { resetDb } from "@portfolio/db/testing/reset";
import { createProfile } from "@portfolio/db/testing/factories";
import { getHomeData } from "./home";

const prisma = makeTestClient();
beforeEach(() => resetDb(prisma));
afterAll(() => prisma.$disconnect());

test("getHomeData renvoie profil + sections visibles ordonnées", async () => {
  await createProfile(prisma, { fullName: "Yohan Debusscher" });
  await prisma.homeSection.createMany({ data: [
    { key: "hero", order: 0, isVisible: true },
    { key: "profil", title: "Le profil", order: 1, isVisible: true },
    { key: "hidden", order: 2, isVisible: false },
  ] });
  const data = await getHomeData(prisma);
  expect(data.profile?.fullName).toBe("Yohan Debusscher");
  expect(data.sections.map((s) => s.key)).toEqual(["hero", "profil"]); // hidden exclu, ordonné
});
```

**Step 2:** Run: `pnpm --filter web exec dotenv -e ../../.env.test -- vitest run lib/data/home.test.ts` → FAIL.

**Step 3: Implement** (un seul round-trip groupé, `select` sûrs, filtres publics).
```ts
// apps/web/lib/data/home.ts
import type { PrismaClient } from "@portfolio/db";

export async function getHomeData(prisma: PrismaClient) {
  const [profile, sections, kpis, skills, tracks, goals, analyses, projects, settings] = await Promise.all([
    prisma.profile.findFirst(),
    prisma.homeSection.findMany({ where: { isVisible: true }, orderBy: { order: "asc" } }),
    prisma.kpi.findMany({ where: { isVisible: true }, orderBy: { order: "asc" } }),
    prisma.skill.findMany({ orderBy: { order: "asc" } }),
    prisma.careerTrack.findMany({ orderBy: { order: "asc" }, include: { milestones: { orderBy: { order: "asc" } } } }),
    prisma.careerGoal.findMany({ orderBy: { order: "asc" } }),
    prisma.analysis.findMany({ orderBy: { order: "asc" }, include: { items: { orderBy: { order: "asc" } } } }),
    prisma.project.findMany({ where: { status: "PUBLISHED" }, orderBy: { order: "asc" }, select: { id: true, slug: true, title: true, summary: true, type: true, featured: true, technologies: { select: { name: true } } } }),
    prisma.siteSettings.findFirst(),
  ]);
  return { profile, sections, kpis, skills, tracks, goals, analyses, projects, settings };
}
```
> Si `@portfolio/web/...` ou les sous-exports `@portfolio/db/testing/*` n'existent pas, ajouter les `exports` correspondants dans `packages/db/package.json`.

**Step 4:** Run → PASS.

**Step 5:** Commit.
```bash
git add apps/web/lib/data/home.ts apps/web/lib/data/home.test.ts packages/db/package.json
git commit -m "feat(web): home data loader from DB with tests"
```

---

### Task 2: Tokens & styles globaux

**Files:** Modify `apps/web/app/globals.css`

**Step 1:** Porter les tokens et primitives utiles de `mockups/shared.css`
(`:root` variables, reset, `.wrap`, typo, `.reveal`, nav, footer). Adapter aux
conventions Tailwind v4 du projet (`@theme` si déjà configuré) — sinon CSS
variables pures. **Ne pas** copier le contenu, seulement la DA.

**Step 2:** Vérifier le build : `pnpm --filter web build` (ou `dev`) → pas d'erreur CSS.

**Step 3:** Commit.
```bash
git add apps/web/app/globals.css
git commit -m "style(web): port DA tokens from shared.css"
```

---

### Task 3: Layout, Nav, Footer (depuis la DB)

**Files:** Create `apps/web/components/site-nav.tsx`, `apps/web/components/site-footer.tsx`; Modify `apps/web/app/layout.tsx`; Tests `apps/web/components/site-nav.test.tsx`

**Step 1: Write the failing test (RTL, comportement)**
```tsx
import { render, screen } from "@testing-library/react";
import { expect, test } from "vitest";
import { SiteNav } from "./site-nav";

test("la nav affiche les libellés des sections visibles", () => {
  render(<SiteNav brand="Yohan." links={[{ href: "#profil", label: "Profil" }, { href: "#cap", label: "Cap" }]} />);
  expect(screen.getByText("Profil")).toBeInTheDocument();
  expect(screen.getByText("Cap")).toBeInTheDocument();
});
```

**Step 2:** Run (vitest jsdom) → FAIL.

**Step 3: Implement** `SiteNav` (props `brand`, `links`) + `SiteFooter`
(props `headline`, `signature`, `socials`) en portant le markup `nav`/`footer`
de `site-v3.html`. Le burger mobile = petit composant client.

**Step 4:** `layout.tsx` : charge `getHomeData`, dérive `links` depuis
`sections.navLabel ?? key`, `brand`/footer depuis `settings`/`profile`. Run le test → PASS.

**Step 5:** Commit.
```bash
git add apps/web/components/site-nav.tsx apps/web/components/site-footer.tsx apps/web/components/site-nav.test.tsx apps/web/app/layout.tsx
git commit -m "feat(web): DB-driven nav and footer"
```

---

### Task 4: Hero (Profil + typewriter)

**Files:** Create `apps/web/components/sections/hero.tsx`, `apps/web/components/typewriter.tsx` (client), Test `apps/web/components/typewriter.test.tsx`

**Step 1:** Test du typewriter sans saut (logique de découpe `typed`/`ghost`).
```tsx
// teste la fonction pure de découpe, pas le timing
import { expect, test } from "vitest";
import { sliceTyped } from "./typewriter";
test("sliceTyped renvoie visible + reste", () => {
  expect(sliceTyped("Je conçois", 3)).toEqual({ typed: "Je ", ghost: "conçois" });
});
```

**Step 2:** Run → FAIL.

**Step 3: Implement** `sliceTyped` + le composant client `Typewriter`
(`lines: string[]`) repris de `site-v3.html` (technique `typed`/`ghost` anti-saut),
respect `prefers-reduced-motion`. `Hero` (server) reçoit `profile` et passe
`profile.typewriterLines` + duotone (photo) + `sigText`.

**Step 4:** Run → PASS.

**Step 5:** Commit.
```bash
git add apps/web/components/sections/hero.tsx apps/web/components/typewriter.tsx apps/web/components/typewriter.test.tsx
git commit -m "feat(web): hero section with jump-free typewriter"
```

---

### Task 5: Sections Profil, Écosystème, Parcours, Cap, Projets

> Une sous-tâche par section ; chacune : composant serveur + (si besoin) test RTL
> de comportement + portage markup depuis les maquettes. Données déjà chargées
> par `getHomeData`.

**5a — Profil** (`components/sections/profil.tsx`) : KPIs (stat-cards) +
SWOT/PESTEL/PORTER depuis `analyses` (mapper `type`→bloc, `items` groupés par
`groupLabel`). Test : un `Analysis` SWOT avec 1 item « Forces » rend la puce.

**5b — Écosystème** (`components/sections/ecosysteme.tsx`) : orbit (noyau + skills
en anneaux), markup de `mockups/orbit.html`/`site-v3.html`. Projets = liens vers `#work`.

**5c — Parcours** (`components/sections/parcours.tsx` + îlot client pour l'anim) :
porter `mockups/parcours-demo7.html` (4 voies, axe temps, convergence, **mobile**),
alimenté par `tracks` + `milestones` + `profile.currentRole`. Test : 1 track « Dev »
avec 1 milestone rend le rôle.

**5d — Cap** (`components/sections/cap.tsx`) : liste checkée depuis `goals`
(statut→case). Test : un goal `ACHIEVED` rend la coche.

**5e — Projets** (`components/sections/projets.tsx`) : scènes depuis `projects`,
chaque carte `→ /projets/[slug]`.

Pour chaque : **test d'abord** (rendu d'un cas minimal), implémentation, test vert, commit
`feat(web): <section> section from DB`.

---

### Task 6: Assemblage `app/page.tsx`

**Files:** Modify `apps/web/app/page.tsx`

**Step 1:** Charger `getHomeData(prisma)` puis composer les sections selon
`sections` (ordre + visibilité). Sections sans données → ne pas rendre.

**Step 2:** `pnpm --filter web build` → OK.

**Step 3:** Commit `feat(web): compose home page from ordered sections`.

---

### Task 7: E2E — la home rend le contenu seedé

**Files:** Create `e2e/home.spec.ts`

**Step 1: Write the test**
```ts
import { expect, test } from "@playwright/test";
test("la home affiche le nom et au moins un projet", async ({ page }) => {
  await page.goto("/");
  await expect(page.getByRole("heading", { level: 1 })).toBeVisible();
  await expect(page.getByText("Domestic Revolt")).toBeVisible(); // issu du seed
});
```
> Pré-requis : la DB de dev est seedée (`seed-content.ts`), `web` lit en `app_web`.

**Step 2:** Run: `pnpm test:e2e e2e/home.spec.ts` → doit passer une fois P2 implémenté.

**Step 3:** Commit `test(e2e): home renders seeded content`.

---

## Definition of Done (P2)

- `apps/web` rend une home complète **depuis la DB**, ordonnée par `HomeSection`,
  sans contenu en dur.
- Loaders et composants couverts par des tests unit (Vitest+RTL).
- E2E : la home affiche le contenu seedé.
- Lecture en rôle `app_web` (filtres `PUBLISHED`/`APPROVED`, `select` sûrs).
- DA fidèle à `site-v3.html`.
