# Copier en `.env` (git-ignoré) et renseigner la vraie valeur.
# En local hors Docker, pointer sur le Postgres du compose (port interne via réseau Docker,
# ou un port publié dédié si tu en exposes un pour les outils).
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/portfolio?schema=public"
